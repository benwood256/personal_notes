#+TITLE: Recap List Page

* Web Development (LEARN 1.12)

** HTML

*Recap List*

1. What does this line mean in the <head> -> <meta charset="UTF-8">
2. What does this line mean in the <head> -> <meta name="viewport" content="width=device-width, initial-scale=1.0">
3. Create a line break or a horizontal line break
4. Add a link which opens in a new page
5. How do you do <nav> & where does it go
6. What's <em> & <strong>
7. Tags to create a collapsible section (i.e. Q & A)
   

*Recap Answers*

1. meta stands for metadata
   The character encoder, it tells the browser that the document is encoded using UTF-8
   This allows text from other languages to display correctly

2. Controls how the page is displayed on mobile devices
   Content sets the width to match the devices width & scale sets zoom to 100% when page first loads
   
3. <br>
   <hr> -> break with a horizontal line in the web page

4. <a href="https://google.com target="_blank">Google Link</a>

5. Within the <header>
   <nav aria-label="primary navigation">
      <a href="#main-section">main title</a> |
      <a href="#additional-section">section title</a> |
      <a href="#content">Contact Info</a>

      Then add the id="main-section" tag to the <section> or <footer>

6. <em>     -> italic
   <strong> -> bold

7. <details> with a <summary> inside
   

** CSS (ADD WAY MORE)

 *Recap List*

1. What unit should I use to size fonts, padding, borders, etc...
2. Link a html file to a css one
3. Name each of the below elements:
   h1 {
        text-align: center;
        font-size: 50px;
    }
4. Center a div horizontally & vertically using CSS
5. What's the difference between margin & padding
6. How do you make a website layout responsive
7. Create a hover animation for a button
8. Whats the difference between position: relative & position: absolute
9. Whats the difference between position & display


 *Recap Answers*

1. Everything should use rem, then it's all relative to one another & is also dynamically sized
   rem = root em -> its based on the font size of the root element (which is <html>)
   1 rem usually is equal to 16px

2. <link rel="stylesheet" href="style.css">

3. h1                     -> selector
   text-align & font-size -> properties
   center & 50px          -> values

4. .container {
      display: flex;
      justify-content: center;    # centers horizontally
      align-items: center;        # centers vertically
      height: 100vh;              # take up 100% of viewport height (allowing align-item to work)
   }

5. Margin  = space outside an element
   Padding = space inside an element, between content & border

6. Use flexible units & media queries

   .card {
      width: 80%;                 # this element takes up 80% of it's parents container width (i.e. screen width)
   }

   @media (max-width: 600px) {    # run this code if the screen is 600px or smaller
      .card {
         width: 100%;             # the card element becomes 100% width (instead of 80) on small devices
      }
   }

7. button {                       # applies style to the button (i.e. when the mouse isn't hovered over it)
      background: blue;           # button background is blue
      transition: 0.3s;           # when a property changes, animate it over 0.3 seconds
   }

   button: hover {                # apply this code when the mouse is over the button
      background: red;            # change the colour from blue to red
      transform: scale(1.1);      # scale the button to 110% it's size
   }

   # this creates a smooth hover effect on a button

8. Relative    -> element stays in normal flow but can be moved
   Absolute    -> removed from normal flow & positioned relative to the nearest parent

   Normal Flow -> the browser behaves as if the element isn't there (i.e. if a, b & c are horizontally in order & b is moved
                  10 pixels to the right, in absolute, c would jump up & take its spot) 

9. Position -> how is the element positioned
   Display  -> how does this element interact with surrounding elements (block, flex, grid & inline)


** Responsive Design

 *Recap List*

1. What is responsive design
2. What is a media query
3. What's the difference between fixed, fluid & responsive layouts
4. When would you use Flexbox vs Grid & how do they work
5. How do you set up an element to be Flexbox & make items go down in columns
6. How do you set up an element to be Grid, 2 equal horizontal elements & a gap between each column
 

 *Recap Answers*

1. Making a website adapt to different screen sizes (PC, laptop, tablet & mobile)

2. Lets you apply CSS code only when a certain screen condition is true (i.e. a breakpoint)

3. Fixed             -> Uses set sizes, doesn't change with screen size
   Fluid             -> Uses flexible sizes
   Responsive layout -> Combines flexible size & media queries
   
4. Flexbox -> Arrange items in 1 direction
   Grid    -> Arrange items vertically & horizontally

   Flexbox would be used if you wanted an item in 1 column going down the page (i.e. a product, then below that another product, etc...)
   Grid would be used if you wanted those products to also sit next to each other
   i.e. a product, next to another, then down to 2 more products

5. .container {
      display: flex;             # creates Flexbox container
      flex-direction: column;    # changes the direction to columns
      justify-content: center;   # centers items in the container horizontally
      align-items: center;       # centers items in the container vertically
   }

6. .container {
      display: grid;                           # creates Grid container
      grid-template-columns: repeat(2, 1fr);   # 2 items next to each other, each taking up 1 fraction of space (equal)
      gap: 20px;                               # 20 pixel gap between columns (i.e. 2 items next to each other, 20 pixels, 2 items, etc...)
   }


** Accessibility

 *Recap List*

1. What is accessibility in web development
2. What can you add to an image that a screen reader can use
3. What tag can you add to an input to make it clear this is for a form


 *Recap Answers*

1. Designing websites so that screen readers can understand them
2. An alt tag with a simple description of what the image is
3. required aria-required="true" -> lets screen readers know this needs to be filled in


** Bootstrap

 *Recap List*

1. What is npm
2. Summarise Bootstrap
3. What's the difference between a library & a framework
4. What are the 3 classes that create a bootstrap structure
5. Difference between col-sm-4, col-md-4 & col-xl-4
6. How do you tell HTML to use Bootstrap
7. How do you add margin below a row
8. How do you make an image responsive
9. How do you create buttons
10. What does form-control do and where does it go
11. In a form, how do you align a label
   

 *Recap Answers*

1. Node Package Manager -> used to install & manage JavaScript packages
   
2. A framework for CSS used for responsive design
   
3. A library is a collection of functions that you call
   A framework provides a structure for building an application & it calls your code
   It defines the architecture, rules & structure, you just fill in the details it requires
   
4. class="container" -> creates correct margins & spacing for the whole document (assuming this goes in <body>)
   Within container: class="row" -> creates the horizontal single row
   Within row: class="col-md-6" -> creates a column - medium size - 6 columns of 12
   
5. Each, before the breakpoint will take up 4 of the 12 columns of space available in the row
   sm (small) -> designed for phones, on every device, 4 of 12 columns will be used
   md (medium) -> designed for tablets, on a tablet or larger 4 of 12 columns will be used, on a phone 12 columns of space will be used
   xl (extra large) -> only on a very large screen will 4 columns be used, any other size 12 columns will be used

   This is a hard line, even with col-md-1, on a phone (i.e. after the breakpoint), all 12 columns will be used
   
6. Add the <link> in the <head> but make sure it's before the link to the style.css
   You can also add the Bootstrap JS file (placed just before </body>) if you're using the interactive Bootstrap components
   
7. You can add <div class="row mb-4">
   Or <div class="col-md-4 mb-4">
   Stands for margin border 4 (size goes from 1 (small) to 5 (very large))

   Adding it to a row, it will add the border between rows
   Adding it to the column, also means the border is between the rows, but it means if the screen goes to narrow & the columns are stacked, it'll add the border between them as well
   So, for <main> add it to the column but for <form> add it to the row (as each item should be on it's own row)
   
8. class="img-fluid"
   
9. Within a <div class="col-md-4">
   <button class="btn btn-danger">Buy</button>
   btn specifies this is a button, btn-danger is the colour (red for this specific label)
    
10. In a <form>, add form-control within an <input> to ensure the input is aligned in Bootstraps standard format (centered correctly)
    
11. class="col-form-label" -> ensures labels align nicely inside Bootstraps grid forms

    
** JavaScript Syntax

 *Recap List*

1. Create a user input
2. Print text & a variable
3. Create a pop alert of your name
4. Link a html & JavaScript file
5. Difference between double & triple equals signs
6. or & and in an if statement
7. Check type of data
8. Convert number_variable to a string


 *Recap Answers*

1. let myName = prompt("enter your name: ")
2. console.log(`your name is: ${myName}`)
3. alert(`your name is: ${myName}`) -> the tilda keys are only required because I'm including a variable (otherwise use speech marks)
4. Before the </body> element add:
   ~<script type="text/javascript" src="main.js"></script>~
5. double -> converts data type (if needed) to compare the 2 inputs (converts string "5" to a number, compares to 5 & returns true)
   triple -> doesn't convert data type (compares string "5", to number 5 & returns false)
6. || -> or
   && -> and
7. typeof(variable)
8. String(number_variable)


** JavaScript Loops

 *Recap List*

1. Create a loop to print a every 3rd number up to 20
2. To the same loop, add a break when i is 12
3. Create a while loop that counts to 10
4. When should you use switch statements & when should you use if statements
5. What is a Do While loop
6. Loop through & print all values in an array
7. Return the index of a value in the array
8. Use 2 different for loops to return every value in the array (use i & of)
9. The same as above but use forEach
10. Create a while loop that flips the order of a given string


 *Recap Answers*

1. for (let i = 1; i < 20; i+=3) {
     console.log(i)
   }
   
2. for (let i = 1; i < 20; i+=3) {
     console.log(i)
     if (i ===== 12) {
        break
     }
   }

3. let counter = 1

   while (counter <= 10) {
       console.log(counter)
       counter++
   } 

4. Switch -> when I'm comparing against many exact values

   *Example*

   let variable day = "Tuesday"

   switch (day) {
       case "Monday":
           console.log("work")
           break
       case "Tuesday":
           console.log("dentist")
           break
       etc...

   If statements -> When conditions are more complex than checking equality

   *Example*

   let age = 15

   if (age >= 18) {
       console.log("Adult")
   }

5. It's the same functionally as a while loop, but the condition is checked at the end so it's code is carried out at least once

6. let colors = ["green", "blue", "purple"]

   for (let color of colors) {
       console.log(color)
   }

7. console.log(colors.indexOf("blue"))     # the 'o' is capitalised, this is camel case JavaScript

8. 

for (let i = 0; i < colors.length; i++) {  # i has to equal 0
    console.log(colors[i]);                # colors.length is 3 (if there's 3 list items), so this stops at 2 (last index)  
}

for (let value of colors) {
    console.log(value);
}

9.

colors.forEach(function (element) {
    console.log(element);
})

10.

let userInput = prompt("enter a word: ");
let reversedInput = "";

let counter = userInput.length - 1;

while (counter >= 0) {
    reversedInput += userInput[counter];
    counter --;
}

console.log(reversedInput, userInput)


** JavaScript Functions

 *Recap List*

1. Difference between function expression & declaration
2. What is a top-level function
3. What is a closure
4. Call this function:
   
    function multiplier(x) {
        function inside(y) {
            return x * y;
        }
        return inside;
    }


 *Recap Answers*

1. A declaration is a normal function which can be called from anywhere in the code -> function hello()
   An expression is when you create a variable & assign an anonymous function to it -> let hello = function ()
   Both are called in the same way 'hello()' but the expression can only be called after it was created (code wise)
2. A function that's on it's own & can be accessed from anywhere in the code (not nested inside another one)
3. A closure is when a nested function (inner function) can store the value of a variable (encapsulation)
4. const multiplyByThree = multiplier(3);
   console.log(multiplyByThree(5));    


** JavaScript OOP

 *Recap List*

1. What is the difference between a class & an object
2. Create a basic inheritance using Dog & Animal classes
3. What are getters & setters
4. Create a simple example of a class Person, use a getter & setter for the name
5. Create an object from the Person class
6. Use the getter to display the persons name
7. Use the setter to change the name & then display the new name
   


 *Recap Answers*

1. You create a class called 'person' & then create the object from the class

2. class Animal {
      constructor(name) {
         this.name = name;
      }
      speak() {
         console.log(`${this.name} makes a sound`);
      }
   }

   class Dog extends Animal {
      bark() {
         console.log(`${this.name} barks');
      }
   }

   let dog1 = new Dog("buddy");
   dog1.speak();     returns -> buddy makes a sound
   dog1.bark();      returns -> buddy barks

3. They are a form of encapsulation, you need to create methods to retrieve the name (getter) or change the name (setter)

   Using getters & setters allows you to isolate a variable (from within a class) so you can change or modify them
   You can make it so this value can't be changed, if it is changed it will return a statement to the terminal, etc...

   After an object is made (from the blueprint/class), a variable can be changed to something invalid, i.e. person1.name = "";
   getters & setters protect the variable from being changed, so it can only be changed in a protected/specific way

4. class Person {
      constructor(name) {
         this._name = name;
      }

      get name() {
         return this._name;
      }

      set name(newName) {
         if (newName.length > 0) {
            this._name = newName;
         } else {
            console.log("Invalid name");
         }
      }
   }

   ~The getter returns the person's name, and the setter changes the person's name after checking that the new name is valid~

5. const new_person = new Person("Geoff");

6. console.log(new_person.name); output -> Geoff

7. new_person.name = "Mark";
   // then call the getter again
   console.log(new_person.name); 

   
** DOM Manipulation

 *Recap List*

1. What does textContent() do
2. Syntax to create a new DOM element
3. What's the difference between getElementById() & querySelector()
4. Give an element a class
5. Add a list item to <ul id="ul-list">, add text to it & add it to ul-list


 *Recap Answers*

1. Sets plain text when placing an element in the HTML file

2. createElement()

3. You can use them for the same thing

   getElementById("ul-list") -> get items only from an ID
   querySelector("#ul-list") -> get any CSS selector (i.e. classes, IDs & elements)

4. ~const listItem = document.createElement("li");~
   ~listItem.textContent = "new list item created";~
   ~listItem.classList.add("new-class-created");         # classList.add()~

5. ~const grabList = document.getElementById("ul-list");~
   ~const listItem = document.createElement("li");~

   ~listItem.textContent = "new list item created";~
   ~grabList.appendChild("li");                          #appendChild()~

   
** Event Handling

 *Recap List*

1. What's the difference between DOM Manipulation & Event Handling
2. What is inline events & how do you use them
3. What does document mean
4. How do you create an alert for if a box is clicked
5. Have do you select 1 element or multiple
6. How would you create a <li> element
7. What's the difference between ~classList.add() & className =~
8. Add a class name to this element
9. Add text to this element
10. How would you check a function is working properly in the web page
11. Add an event which runs a function when you click 'Enter'
12. What does ~element.innerHTML =~ do & how's it different to element.textContent =
    Give an example of using .innerHTML


 *Recap Answers*

1. DOM Manipulation -> changing the structure, content or style of a web page
   Event Handling   -> listening for user/browser actions & running code when they happen

   addEventListener() is specific to Event Handling as this tracks an 'event'

2. This is where you put the tag next to the element in HTML & assign it an outcome

        <div id="box" onclick="alert('hey dude')"></div>

    However, don't use this method as it's very limited & you can't do a whole lot (stick to creating the elements in JavaScript)

3. The html script, its a way of using querySelector & getElementById by doing document.querySelector()

4. let boxDiv = document.getElementById("box");
   boxDiv.addEventListener("click", function() {
       alert("hey dude");
   })

   This has the exact same effect as the <div> code in the example above
   
5. querySelector("#name")   -> select the first element, name can be element, ID or class name
   querySelectorAll("#name") -> select all elements -> will need to be followed by a forEach loop

6. let listItem = document.createElement("li");
   Document refers to the HTML script & "li" creates <li>

7. ~classList.add() -> adds a class next to any other classes the element might have~
   ~className       -> overwrites all classes with this~

8. listItem.className = "list-item";  // list-item can be used in CSS (.list-item { )

9. listItem.textContent = "shrek";    // the word shrek will be in the web page now

10. In JavaScript, after the function is created, add a console.log("<function name> is called")
    Then, after the action (i.e. a button is clicked), in the console it'll show if the function has actually been called or not

11. ~document.addEventListener("keydown", function(e) {~

    ~if (e.key ===== "Enter") {~
        ~runFunction();                             # if Enter is clicked, run a function (i.e. add this text to a box)~

12. .innerHTML   -> lets you set HTML elements (i.e. <b> or <li>) & create text
    .textContent -> you can only set the text inside the html

    element.innerHTML = "<h1>Hello</h1>";  # if the element was an empty <div></div> it would now be:
    <div><h1>hello</h1></div>


** Promises, Async & Await (LEARN)

*Recap List*

1. What is a Promise in JavaScript?
2. What are the three states of a Promise?
3. How do you create a new Promise?
4. What does the resolve() function do?
5. What does the reject() function do?
6. What is the purpose of the .then() method?
7. What is the purpose of the .catch() method?
8. What is the purpose of the .finally() method?
9. What is promise chaining, and why is it useful?
10. How do you run multiple promises at the same time using Promise.all()?
11. What happens if one promise fails when using Promise.all()?
12. What is the difference between Promise.all() and Promise.allSettled()?
13. What is the purpose of the async keyword?
14. What does an async function always return?
15. What is the purpose of the await keyword?
16. Where can the await keyword be used?
17. How do you handle errors when using async and await?
18. What is the difference between using .then() and using async/await?
19. Why is await often easier to read than chaining multiple .then() calls?
20. Write an async function that fetches data from an API using fetch() and await, then logs the JSON response.

*Recap Answers*

1. A Promise is an object that represents the eventual completion (success) or failure of an asynchronous operation and its resulting value

2. Pending   –> The operation is still in progress
   Fulfilled –> The operation completed successfully
   Rejected  –> The operation failed

3. const promise = new Promise((resolve, reject) => {
        if (true) {
            resolve("Success");
        } else {
            reject("Error");
        }
   });

4. resolve()  -> marks the Promise as fulfilled and returns a successful result

5. reject()   -> marks the Promise as rejected and returns an error or reason for failure.

6. .then()    -> runs code after a Promise has been fulfilled and receives the resolved value

7. .catch()   -> handles errors if a Promise is rejected

8. .finally() -> runs code after a Promise finishes, whether it succeeds or fails

9. Promise chaining links multiple .then() methods together so each step waits for the previous one to finish

10. Promise.all([promise1, promise2, promise3])
        .then(results => {
            console.log(results);
        });

11. Promise.all() immediately rejects, and the remaining results are ignored

12. Promise.all() stops if any Promise rejects
    Promise.allSettled() waits for all Promises to finish, regardless of whether they succeed or fail

13. The async keyword declares a function as asynchronous, allowing the use of await inside it

    async function getData() {
    }

14. An async function always returns a Promise, even if it returns a normal value

        async function example() {
            return 10;
        }

    Returns:

        Promise.resolve(10);

15. await pauses the execution of an async function until a Promise has been fulfilled or rejected

    const response = await fetch(url);

16. await can only be used inside an async function (or at the top level of supported JavaScript modules)

17. Use a try...catch block

        async function getData() {
            try {
                const response = await fetch(url);
            } catch (error) {
                console.log(error);
            }
        }

18. async/await makes asynchronous code look more like synchronous code, making it easier to read
    .then() uses callbacks to handle Promise results

19. await allows code to be written in a top-to-bottom order, reducing nested callbacks
    and making complex asynchronous code easier to understand and debug

20. async function getUsers() {
        try {
            const response = await fetch("https://jsonplaceholder.typicode.com/users");
            const data = await response.json();
            console.log(data);
        } catch (error) {
            console.log(error);
        }
    }

    getUsers();


* Software Engineering (LEARN 2.2, 2.3, 2.6, 2.7 & 2.8)
** System Architecture

 *Recap List*

1. Give an example of Layered Architecture & why
2. Give an example of Repository Architecture & why
3. Give an example of Client-server Architecture & why
4. Give an example of Pipe & Filter Architecture & why
5. Give a 2 pros & cons for using multiple systems


 *Recap Answers*

1. _Microsoft Outlook_ has a user interface (emails & calendar) then a layer which handles email management, then a data access layer
   & finally a layer to store emails, contacts, etc... in the form of data

2. _Git_ uses a central repository which contains all the data for the software

3. _Meta_ has apps & websites which are used by many customers, they then rely on a large server of data to provide the functionality to the applications

4. _Unix_ system (or Linux) as data can manipulated in complex ways (or through many individual steps)

5. pros -> scalable & allows for a flexible system
   cons -> very complicated system, therefore also expensive & difficult to maintain


** Network Protocols & HTTP Messages (LEARN)

 *Recap List*

1. What is a network protocol
2. What is TCP
3. What is UDP
4. What is DNS
5. What is FTP used for
6. What is SMTP
7. What's the difference between a protocol & a port
8. Give a simple summary the relationship between a client & server, when the client enters a URL
9. How does https work to securely sends data
10. What is a HTTP message
11. Break down the 3 parts of a HTTP Request message
12. Break down the 3 parts of a HTTP Response message
13. Explain what each status code means (100s, 200s, 300s, 400s & 500s)
14. Define -> GET, POST, DELETE, PUT & PATCH


 *Recap Answers*

1. A network protocol is a set of rules that define how data is transmitted and received between devices on a network

2. TCP (Transmission Control Protocol) is a reliable protocol that ensures data is delivered accurately and in order

3. UDP (User Datagram Protocol) is a faster but less reliable protocol that sends data without checking for errors or order

4. DNS (Domain Name System) translates website names (a url) into IP addresses

5. FTP (File Transfer Protocol) is used to transfer files between a client and a server over a network

6. SMTP (Simple Mail Transfer Protocol) is used to send emails between servers

7. Protocol → rules for communication (e.g. HTTP, TCP)
   Port → a numbered endpoint used to direct traffic (e.g. port 80 for HTTP)

8. A client (your browser) wants to load a website
   The browser asks DNS to translate the domain name (i.e. ~https://google.com~) into an IP address
   DNS returns the server's IP address
   Your browser connects to the server (hosting place of the website)
   The server sends the website data back to your browser (DNS isn't needed as the server uses your IP address)

9. Server gives you a public key (they send it with the certificate it's a padlock anyone can close but only the server can open)
   The browser generates a random secret key
   You encrypt this secret key using the server's public key (meaning the server is the only one that can decrypt it)
   So when the data is sent over, it's encrypted with the public key
   The server receives this & decrypts it's own encryption with the public key to receive the secret key
   Then they can decrypt the data using the secret key

10. There's a Request HTTP message when a client requests data from a server
    & a Response HTTP message when the server sends its reply

11. The method -> GET, POST, DELETE, etc...
    The path   -> the location of the data (i.e. /index.html)
    The HTTP Version

12. The HTTP version
    The status code
    The status message

13. 100s -> the request is continuing
    200s -> success codes
    300s -> redirection codes
    400s -> client error codes (there was a problem)
    500s -> server error codes (the request was accepted but there was an error on the server side)

14. GET    -> retrieve data from a server
    POST   -> send data to input into the server
    DELETE -> remove a resource
    PUT    -> update or replace an existing source
    PATCH  -> partially update a resource


** JSON & Web Storage APIs (LEARN)

 *Recap List*

1. What does parse mean
2. Why convert code to a JSON file
3. Check what's stored in localStorage
4. The functions to convert a JS object to a json string & the reverse
5. Explain this line:
   ~let storedItem = JSON.parse(localStorage.getItem("storedItem")) || {};~
6. Create a cookie
7. What is localStorage used for
8. What's a data requirement of using localStorage
9. Create an item in localStorage & retrieve the item
10. What is cache used for
11. How do you create a cache
12. When would you use: cookies, sessionStorage, localStorage & cache storage


 *Recap Answers*

1. To take data in 1 format & convert it into a format that a program can understand

2. So we can convert data between client & server in a standardised format
   Everything that is transferred via the web uses HTTP, which can only transfer text
   JSON file converts code to strings

3. F12, Application, Storage, Local Storage
   
4. ~JSON.stringify(json_object)~ -> JavaScript object to a string
   ~JSON.parse(json_text)~       -> JSON string to JavaScript object

5. Get the "storedItem" value from localStorage
   Converts the JSON string to a JavaScript object
   If no settings exists, create an empty object

6. function cookieCreate(name) {
       document.cookie = "username=" + name + ";max-age=86400;path=/";

   document.cookie -> creates cookie
   "username="     -> saves the name of the cookie (needs to be a key:value pair, so you can retrieve it)
   max-age         -> creates length of time the cookie will be stored (in seconds)

7. Its a built in JS API for storing data that persists after the browser is closed
   Its used for storing user settings & saving application state

8. It only stores strings so you have to convert JavaScript objects to JSON strings for it to be saved
   & then convert them back again to use them in a script

9. localStorage.setItem("theme", "dark");     # saves data to local storage as a key, value pair
   let theme = localStorage.getItem("theme"); # retrieve the data using the key to get the value
   console.log(theme);                        # output -> dark

10. Temporarily storing resources (images, scripts or data to improve performance so files don't need to be re-downloaded)

11. You use a service worker (create a sw.js file)
    In this file you create function & event listeners that download files & reuse them when a web page is loaded

12. Cookies        -> where you need to share data between the client & server (mainly store authentication tokens or user preferences) 
    sessionStorage -> storing temporary data that only needs to exist for the duration of a single session
    localStorage   -> data that need to remain after the browser is closed (main use is user preferences)
    Cache storage  -> used to enhance performance by storing resources such as images, CSS files & JavaScript in the browser
                      so they can be accessed quickly without needing to re-download them from the server


** Shell Scripting

 *Recap List*

1. What's the difference between $@ & $*
2. Create a variable & print it to the terminal
3. Create a user input & print it to the terminal
4. Create basic layout of an if statement with an else if & else
5. If statement conditions for equal to, greater than & less than or equal to
6. syntax to combine 2 statements & the syntax to do an *or* statement
7. What does the command 'exit 6' mean
8. What's used to define a variable in a function
9. Create a function with 1 argument & call the function & pass an argument


 *Recap Answers*

1. Both are command-line arguments
    $@ -> keeps arguments separate               (i.e. apple banana orange would all be on thir own line)
    $* -> combines all arguments into one string (i.e. apple banana orange next to each other)
    
2. fact="linux"
   echo $fact    returns -> linux

3. echo "type a number:"
   read number
   echo $number   returns -> (the number the user entered)

4. ~if [[condition]]; then~
       statement
   ~elif [[condition]]; then~
       statement
   else
       statement
   fi

5. -eq -> equal to
   -gt -> greater than
   -le -> less than or equal to

6. -a  -> and
   -o  -> or

7. Exit out of the file & return the output 6 (indicting exit code 6)

8. $1 -> first variable
   $2 -> second variable (& so on)

   So, in a function (echo "Hello there $1, you have $2 money in your account")

9. greet() {
      echo "Hello $1"  # $1 is the first argument passed, $2 would be number 2 & so on 
   }

   greet Alex          # output -> Hello Alex




** Relational Databases

 *Recap List*

1. What is normalisation
2. What are redundancies
3. What is a primary key (& the alternative name) & a composite key
4. What are dependencies
5. Explain partial & transitive dependencies
6. Use this table, convert it to first normal form (1NF)

   | StudentID | StudentName | CourseID   | Courses       | InstructorNames      | InstructorPhones   |
   |           |             |            |               |                      |                    |
   |       101 | Alice       | C001, C003 | Math, History | Mr. Smith, Dr. Brown | 555-1234, 555-9876 |
   |       102 | Bob         | C002       | Science       | Mrs. Jones           | 555-5678           |
   
7. What is the primary key
8. Convert it further to second normal form (2NF)
9. Convert it further third normal form (3NF)
   

   *Recap Answers*

1. A way of reformatting a database in an attempt to reduce redundancies

2. Duplicated data stored in many locations, then, as data is unorganised, when data is updated, it leaves the copies out of date

3. The thing that uniquely identifies a row (an ID) -> also know as a prime attribute
   Composite key -> multiple identifiers are needed

4. A way of connecting attributes, i.e. StudentID is 6, so you know that StudentName is "Bob" (StudentName is dependent on StudentID)

5. Partial    -> only occurs when the key is composite
                 partial dependency is when 1 of the columns of the composite key, links to an attribute
                 example: composite key is ProductID & NameID, but Product name can be found if you have the ProductID (partial dependency)

   Transitive -> can occur if the key is simple or composite
                 when a non-prime attribute depends on another non-prime attribute, which in turn depends on the primary key
                 example: key is ProductID, ProductName is dependent on ProductID & ProductPrice is dependent on ProductName
                          but you can't get the ProductPrice from the ProductID                 

6. Split up cells into new rows which contain multiple items/values
   
   | StudentID | StudentName | CourseID | Courses | InstructorNames | InstructorPhones |
   |           |             |          |         |                 |                  |
   |       101 | Alice       | C001     | Math    | Mr. Smith       |         555-1234 |
   |       101 | Alice       | C003     | History | Dr. Brown       |         555-9876 |
   |       102 | Bob         | C002     | Science | Mrs. Jones      |         555-5678 |

7. StudentID & CourseID

8. Remove partial dependency by creating new tables

   Students-Course Table:
   | StudentID | StudentName | CourseID | 
   |           |             |          |
   |       101 | Alice       | C001     |
   |       101 | Alice       | C003     |
   |       102 | Bob         | C002     |

   Courses-Instructors Table (instructor info depends on CourseID):
   | CourseID | CourseName | Instructor | InstructorPhone |
   |          |            |            |                 |
   | C001     | Math       | Mr.Smith   |        555-1234 |
   | C002     | Science    | Mrs. Jones |        555-5678 |
   | C003     | History    | Dr. Brown  |        555-9876 |

9. Remove transitive dependency
   InstructorPhone -> Instructor -> CourseID
   i.e. InstructorPhone depends on Instructor, which depends on CourseID

   Students-Course Table, same as the above

   Courses Table:
   | CourseID | CourseName | InstructorID |
   |          |            |              |
   |        1 | Math       |            1 |
   |        2 | History    |            2 |
   |        3 | Science    |            3 |

   Instructors Table:
   | InstructorID | InstructorName | InstructorPhone |
   |              |                |                 |
   |            1 | Mr. Smith      |        555-1234 |
   |            2 | Dr. Brown      |        555-9876 |
   |            3 | Mrs. Jones     |        555-5678 |

   StudentsCourses Table:
   | StudentID | CourseID |
   |           |          |
   |       101 |        1 |
   |       101 |        2 |
   |       102 |        3 |






** SQL (LEARN)

*Recap List*

1. What is SQL, and what is it used for
2. What is the difference between the WHERE clause and the HAVING clause
3. What is a primary key in SQL
4. What is a foreign key in SQL, and why is it used
5. What is the purpose of the SELECT statement in SQL
6. What does the DISTINCT keyword do in SQL
7. What is the difference between DELETE, TRUNCATE, and DROP in SQL
8. What is the purpose of the ORDER BY clause in SQL
9. What is the purpose of the GROUP BY clause in SQL
10. What are aggregate functions in SQL, and what are some examples
11. What is an INNER JOIN in SQL, and when would you use it
12. What is a LEFT JOIN in SQL, and how is it different from an INNER JOIN
13. What is the difference between UNION and UNION ALL in SQL
14. What does NULL mean in SQL
15. Why do we use IS NULL instead of = NULL in SQL
16. What is a subquery in SQL, and when would you use one
17. What is the difference between IN and EXISTS in SQL
18. What is an index in SQL, and why is it useful
19. What is a Common Table Expression (CTE) in SQL
20. What is the logical order in which SQL clauses are executed

*Recap Answers*

1. SQL stands for Structured Query Language. It is used to communicate with and manage relational databases
   SQL can be used to retrieve, insert, update, and delete data, as well as create and modify database structures

2. WHERE filters individual rows before grouping takes place, while HAVING filters groups after GROUP BY has been applied

3. A primary key is a column, or combination of columns, that uniquely identifies each row in a table.
   It must contain unique values and cannot contain NULL values

4. A foreign key is a column that creates a relationship between two tables.
   It usually references the primary key of another table and helps maintain relationships between tables

5. SELECT is used to retrieve data from a database

6. DISTINCT removes duplicate values from the results returned by a query

7. DELETE removes selected rows and can use a WHERE condition. TRUNCATE removes all rows while keeping the table structure
   DROP removes the entire table, including its structure

8. ORDER BY is used to sort query results. ASC sorts in ascending order, while DESC sorts in descending order

9. GROUP BY groups rows with the same values. It is commonly used with aggregate functions such as COUNT(), SUM(), and AVG()

10. Aggregate functions perform calculations on multiple rows. Common examples are COUNT(), SUM(), AVG(), MIN(), and MAX()

11. An INNER JOIN combines two tables and returns only the rows where a matching value exists in both tables

12. A LEFT JOIN returns all rows from the left table, including rows that do not have a match in the right table
    An INNER JOIN only returns rows where a match exists in both tables

13. UNION combines query results and removes duplicates. UNION ALL combines query results but keeps duplicates

14. NULL represents a missing, unknown, or unavailable value. It is not the same as 0, an empty string, or FALSE

15. We use IS NULL because NULL represents an unknown value and cannot be compared using =

    WHERE column_name IS NUL

    To find values that are not NULL

    WHERE column_name IS NOT NUL

16. A subquery is a query inside another query. It is useful when the result of one query is needed by another query

17. IN checks whether a value matches a value in a list or sub-query result
    EXISTS checks whether a subquery returns at least one row

18. An index is a database structure that helps the database find data more quickly
    Indexes can improve search performance but require additional storage and can slow down INSERT, UPDATE, and DELETE operations

19. A Common Table Expression (CTE) is a temporary named result set that can be referenced within a query
    It is created using the WITH keyword and can make complex queries easier to read

20. The logical order of SQL execution is approximately:

    FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT


** MariaDB (LEARN)


*Recap List*

1. What is MariaDB, and how is it related to MySQL
2. What is the difference between MariaDB and a relational database
3. How do you create a new database in MariaDB
4. How do you create a new table in MariaDB
5. What is the purpose of the AUTO_INCREMENT attribute in MariaDB
6. How do you insert data into a table in MariaDB
7. How do you update existing data in a MariaDB table
8. How do you delete data from a MariaDB table
9. What is the purpose of the LIMIT clause in MariaDB
10. How can you find the number of rows in a MariaDB table

*Recap Answers*

1. MariaDB is an open-source relational database management system (RDBMS)
   It was originally created as a fork of MySQL and is highly compatible with MySQL. It uses SQL to store, manage, and retrieve data

2. MariaDB is a relational database management system, while a relational database is the type of database that
   stores data in structured tables consisting of rows and columns. MariaDB is the software used to create and manage those databases

3. You can create a new database using the CREATE DATABASE statement

    CREATE DATABASE company;

    You can then select the database using:

    USE company;

4. You can create a new table using the CREATE TABLE statement

    For example:

    CREATE TABLE employees (
        employee_id INT PRIMARY KEY,
        name VARCHAR(100),
        salary DECIMAL(10,2)
    );

    This creates an employees table with three columns

5. AUTO_INCREMENT automatically generates a new number for a column when a new row is inserted. It is commonly used with primary keys

    For example:

    CREATE TABLE employees (
        employee_id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(100)
    );

    The database will automatically generate values such as 1, 2, 3, and so on

6. You can insert data into a MariaDB table using the INSERT INTO statement

    For example:

    INSERT INTO employees (name, salary)
    VALUES ('John', 35000);

    This adds a new employee to the table

7. You can update existing data using the UPDATE statement together with the SET and WHERE clauses

    For example:

    UPDATE employees
    SET salary = 40000
    WHERE employee_id = 1;

    The WHERE clause specifies which record should be updated

8. You can delete data using the DELETE FROM statement

    For example:

    DELETE FROM employees
    WHERE employee_id = 1;

    The WHERE clause specifies which row should be deleted. Without a WHERE clause, the statement can delete all rows from the table

9. The LIMIT clause restricts the number of rows returned by a query

    For example:

    SELECT *
    FROM employees
    LIMIT 10;

    This returns a maximum of 10 rows

    It is often useful when you only want to retrieve a small number of records

10. You can use the COUNT() aggregate function to find the number of rows in a table

    For example:

    SELECT COUNT(*)
    FROM employees;

    This returns the total number of rows in the employees table


** NodeJS (LEARN)


*Recap List*

1. What is Node.js, and what is it used for
2. What is the difference between Node.js and JavaScript
3. What is npm, and what is it used for in Node.js
4. What is the purpose of the package.json file in a Node.js project
5. What is a Node.js module, and how can you import one
6. What is the difference between require() and import in Node.js
7. What is the Node.js event loop, and why is it important
8. What is Express.js, and why is it commonly used with Node.js
9. What is middleware in Node.js and Express.js
10. How do you create a basic HTTP server using Node.js

*Recap Answers*

1. Node.js is a JavaScript runtime environment that allows you to run JavaScript outside of a web browser
   It is commonly used to build backend applications, APIs, web servers, and real-time applications

2. JavaScript is a programming language, while Node.js is a runtime environment that allows JavaScript to run outside the browser

   For example, JavaScript can run in a browser to control a webpage, while Node.js can run JavaScript on a server to handle requests,
   access databases, and create APIs.

3. npm stands for Node Package Manager. It is used to install, manage, and share packages and libraries that can be used in Node.js projects

    For example:

    npm install express

    This installs the Express package into your project

4. package.json contains important information about a Node.js project

    It can include:

    The project name and version.
    Dependencies.
    Development dependencies.
    Scripts.
    Project configuration.

    For example:

    {
    "name": "my-app",
    "version": "1.0.0",
    "scripts": {
        "start": "node app.js"
    }
    }

5. A Node.js module is a reusable piece of code that can be imported and used in another file

    Node.js includes built-in modules such as fs, http, and path

    For example:

    const fs = require('fs');

    You can also create your own modules and install third-party modules using npm

6. require() is the traditional CommonJS way of importing modules in Node.js

    Example:

    const express = require('express');

    import is the ES Module syntax.

    Example:

    import express from 'express';

    Modern Node.js supports both approaches, although they use different module systems and configuration

7. The Node.js event loop allows Node.js to handle many operations asynchronously without blocking the main thread

For example, when Node.js needs to read a file or wait for a database response, it can continue handling other tasks
instead of stopping and waiting for that operation to finish

   This is one of the reasons Node.js is well suited to applications that handle many simultaneous requests

8. Express.js is a popular web framework for Node.js. It makes it easier to create web servers and APIs

    For example:

    const express = require('express');

    const app = express();

    app.get('/', (req, res) => {
        res.send('Hello World');
    });

    app.listen(3000);

    Express provides features such as routing, middleware, request handling, and response handling

9. Middleware is a function that runs between receiving a request and sending a response

    Middleware can be used for things such as:

    Authentication
    Logging
    Parsing JSON
    Error handling
    Checking permissions

10. You can create a basic HTTP server in Node.js using the built-in http module

    Example:

    const http = require('http');

    const server = http.createServer((req, res) => {
        res.writeHead(200, {
            'Content-Type': 'text/plain'
        });

        res.end('Hello World');
    });

    server.listen(3000);

    This creates a server that listens on port 3000 and responds with "Hello World" when a request is received


** Setting up React using Vite

*Recap List*

1. What is Vite
2. What is React
3. How do you set up a React app with Vite
4. What does create & vite@latest mean
5. What configuration do I choose when prompted by Vite
6. What's the next step
7. In VS Code, you need to now install dependencies, how & why
8. Then what
9. What is Laravel & how does it compare to React

*Recap Answers*

1. A tool used to create a folder structure for building an interactive website (i.e. the front-end)
   It also configures the folder so I can use a JavaScript library (i.e. React) or a framework
   It provides a development server -> creates a local version of the website I can view in my browser whilst working (like 'live server' in VS Code)

2. A JavaScript library for building user interfaces -> i.e. interactive websites (front-end)
   It gives me JavaScript functions but also offers rules for building the website (i.e. so there's structure to it)

3. cd to where I want the Vite folder structure
   You run: npm create vite@latest <project-name>

4. create      -> tells npm I want to create a new project
   vite@latest -> tells npm which creation tool to use (vite -> vite project generator; @latest -> newest available version)

5. Framework -> React
   Variant   -> JavaScript + React Compiler (the compiler automatically optimises my React code)
   Linter    -> ESLint - this is the default for a JS/React ecosystem (a linter is a tool that checks grammar of programming syntax)

6. Open the project in VS Code

7. npm install (in the <project name> directory)
   This reads the package.json & download everything the project needs

   So, when you create the files & folder structure with vite, it installs package.json
   which includes what I need (i.e. React, React DOM, vite, etc...)
   Then, npm install, reads this package.json & actually downloads those tools in a directory called node_modules/

8. npm run dev -> starts the Vite development server, i.e. the local version of my website

9. Laravel is a back-end framework which uses PHP language
   React focuses on the front-end & uses JavaScript
   Laravel is a framework while React is a library


** React Introduction

*Recap List*

1. In a Vite & React environment, where do I write code for the index.html & what's in the index.html
2. What is in the <script> element of the index.html
3. What's in the main.jsx
4. What is JSX
5. Create a function in JSX, which can be called to give the element <h2> & <p> in a <div> container & then export the function
6. Import & call the new function
7. Where do each of these lines of code go
8. What's the HTML rule in JSX 
9. What's the attribute value rule in JSX
10. How do you use the image man.png in JSX
11. What is the 1 parent element rule in JSX

*Recap Answers*

1. The index.html contains a <div id="root"></div> which links to the src/main.jsx using the id
   This is an empty container where React will place the user interface of the website
   
   I write code in src/App.jsx, which is loaded by the src/main.jsx & inserted into the index.html

2. <script type="module" src="/src/main.jsx"></script>
   This tells the browser to load & run main.jsx

3. It imports the code from App.jsx & also retrieves the code from index.html (so it can insert the App.jsx code)
   It does this with: document.getElementById('root') -> linking to the id in the <div> element from the index.html

4. JavaScript XML -> allows you to write HTML-like code inside JavaScript
                     its used in React to describe what the user interface should look like
                     its pretty much specific to React (or got popularised by it)

5. function ProfileCard() {
      return (
         <div>
            <h2>Alex</h2>
            <p>Developer</p>
         </div>
      )
   }

   export default ProfileCard

6. import ProfileCard from "./components/ProfileCard"
   <ProfileCard />      -> will produce the div container with the h2 & p elements inside

7. The components (i.e. the function) should be within a self-made _./components/ directory_ (next to the main.jsx & App.jsx) & exported from here
   The function is imported & called within _App.jsx_

8. You can use every HTML element & create your own (i.e. <div>, <img> & the function above is a self-made one)

9. Attributes values are usually enclose in speech marks (src="https://www.google.com") but this will be treated as a normal string in JSX
   You call attributes with curly brackets (i.e. instead of src="https://www.google.com" you do src={user.googleLink)
       Then you can create the 'user' variable & change the link whenever & it will be updated for the html
   
10. You must import images by: import profilePicUser from "./profiles/man.png";
    The variable profilePicUser (can be anything) now associates to this image

11.


* Elisp, Bash, JavaScript & Python (EVERYDAY)

** Elisp

 *Recap List*

1. How do you call a function in scratch buffer
2. Define the function with name 'adding_together' & pass 2 parameters 'num1' & 'num2' & code to add them together
3. Call the function and pass 2 numbers to it
4. What is true & false in Elisp
5. What does #' mean in Emacs & where would you use it
6. Create the key-binding C-S-n & assign it to the made up function 'create-life' (convention is to use hyphens in Elisp)
7. Why would 'create-life work as well as #'create-life in the above key-binding function
8. Create the variable 'num' & pass 10 to it
9. Return to console 'num' multiplied by 3
10. What does the ' mean (give 3 examples)
11. Make a function called 'greet', which returns 'hello' & make it interactive (i.e. can be called using M-x)
12. Explain what a symbol is & how this line works -> (require 'evil)
13. What's another way to write -> #'flyspell-mode
14. Create a function that returns "hello world" & assign the shortcut C-S-h to this function
15. How do Elisp if statements work
16. Create an if statement to return 'true' or 'false'
17. Do another one to check if a variable is greater than 10


 *Recap Answers*

1. Go a line after the function is created -> C-x C-e
   Go a line after the function call       -> C-x C-e

2. (defun adding-together (num1 num2)      -> don't close the other bracket, this is closed at the end of the function 
      (+ num1 num2))

3. (adding-together 23 12)

4. true  -> t (although technically, anything that is not nil is considered true but t is standard practice)
   false -> nil

5. #' -> tells Emacs this symbol refers to a function
   So you put this before the function name to tell emacs I'm passing you a function
   #'create-life -> refers to the function (like in JavaScript, myFunction -> if you don't put () you're just referring to it, not calling it) 

6. (keymap-global-set "C-S-n" #'create-life)         ;; control shift n

   (global-set-key (kbd "C-S-n") #'create-life)      ;; identical -> legacy way of setting key-bindings

7. Because keymap-global-set expects a function, so 'create-life is passing Emacs the symbol create-life
   Emacs then looks up the function attached to that symbol

8. (setq num 10)   ;; setq -> built-in function meaning 'set variable'

9. (* num 3)       ;; C-x C-e on the variable & the same on this will return 30

10. Don't evaluate this the following expression -> known as a *quote*

    '(1 2 3) -> treats this as a list, the most common use of this key

    (setq x 10)
     x       -> returns: 10
    'x       -> returns: x

    '(+ 1 2) -> returns: (+ 1 2)

    (keymap-global-set "C-S-h" 'my-function) -> without the ', Emacs would try to evaluate my-function instead of treating it as a symbol

11. (defun greet ()            -> note: there's a space after the function name & before the brackets
        (interactive)          -> this has to be the first thing after the function is made
        (message "hello"))

    When you do M-x greet RET  -> returns: hello

12. symbol -> a variable of sorts that acts as a pointer to: variable, function or feature

    require -> is a built in function which loads this 'feature'
    'evil   -> this is treated as a symbol, not a variable (so it finds the feature named 'evil')

13. (function flyspell-mode)  -> this passes the function it DOESN'T call it

14. (defun print-hello-world ()
        (interactive)                              -> you can call this function from M-x
        (message "hello world"))
        
    (keymap-global-set "C-S-h" #'print-hello-world) -> using this key-binding, call the function (this is not in the function)

    You can do keymap-set which will make the key-binding local (rather than accessible from all modes)

15. There are no else if, there's only an if & an else (there can be just an if -> no else statement)
    (if CONDITION
        THEN              -> the code that runs if the condition is *true*
        ELSE)             -> the code that runs if the condition is *false*

16. (if t                 -> if I swap this to nil it would return: it-was-false
        'it-was-true      
        'it-was-false)

17. (setq x 15)
    (if (> x 10)
        "x is greater than 10"
        "x is equal to 10 or less")


** Bash

<<bash-link>>

 *Recap List*

1. Create a counter, assign 10 to it, then increment it by 5, return the number to the terminal
2. Create a message, append text to it & return it to the terminal
3. What is env & how can you change a value there
4. List a few environment variables
5. Explain .bashrc
6. Explain PATH
7. Append a command to an output.txt
   Redirect errors to errors.txt
   Redirect everything to output.txt
8. What does * do before a name & use an example
9. Explain the difference between &&, || and ;
10. Change permissions of a file to ~rwxrw-r--~
    Change ownership of the same file to john-doe of group developers (how would change all subsequent files too)
11. Find all lines containing the word 'error' in logfile.txt
    Find the emacs PID
12. Kill a process with its PID
    Kill all process with the exact name 'firefox'
    Kill all matching process with 'firefox' in their name
13. Find all files ending in .txt
    Find only files (& then only directories)
    Find a file that is 1000 bytes in size
    Search for a file with the name "emacs.org" from the root (make sure to ignore errors)
14. Find all files ending with .txt & delete them in 1 command
15. Sort the lines of file.txt alphabetically & count how many times a unique line appears 
16. if statement, check if -> a file exists, directory exists, readable, writable, executable

    
 *Recap Answers*

1. count=10 
   count=$((count + 5))
   echo $count

2. declare message="Hello World!"
   message="$ Have a great day!"
   echo $message

3. A command that displays the environment variables (specific to your current shell process)
   The default commands are usually created when you log in (i.e. USER, HOME, SHELL, etc...)
   export EDITOR=nano -> now echo $EDITOR will be 'nano'

4. echo $SHELL -> will be saved as SHELL=/bin/bash in env
   echo $USER  -> will be saved as USER="john-doe" in env
   echo $HOME  -> will be saved as HOME=/home/john-doe in env

5. Every time I open a terminal, this file is run -> essentially its the init.el file for the terminal

   Within it, I can:
   - add environment variables (export EDITOR=emacs)
   - add aliases (i.e. shortcuts) -> alias ll="ls -lah"
                                     now typing ll will be read as "ls -lah" 
   - add extra locations to PATH

6. When you run a command (i.e. ls), bash uses PATH to search for this command in its file paths
   In PATH there are actually multiple file locations, & it will search until it finds a match

   For the 'ls' example, it finds it in /usr/bin/ls & then runs it

   So, you can add a path to a file where you have executables (i.e. ~/bash_script/)
   Then, you can just do ./update_and_upgrade from anywhere, as bash will find this script in the path & then run it)

7. Append              -> command >> output.txt
   Redirect errors     -> command 2> errors.txt
   Redirect everything -> command > output.txt 2>&1

   2>&1 -> the 2 is stderr (errors), & your saying they should be directed to the same location as stdout (standard output)
           stdout is already going to output.txt so errors now follow

           the & is needed because the system could treat 1 as a file name, the & tells it to treat it as a file descriptor not a file name
           this is needed as after a redirect, bash assumes it receiving a file (i.e. 2 > error_output.txt)

8. It means, include all instance of this
   rm *.txt -> would remove all instance of files in this path ending with .txt

9. && -> run next only if successful
   ~||~ -> run next when the 1st command failed
   ;  -> run regardless

10. chmod 764 script.sh
    chown john-doe:developers script.sh (-R to include all files within a directory)

11. grep error logfile.txt
    pgrep emacs              (will return 1234 or 372 or another number)

12. kill 123        -> process with PID of 123
    ~pkill firefox~   -> kills all processes with the EXACT name 'firefox'
    ~killall firefox~ -> kills all processes with the word 'firefox' in their name (e.g. firefox, firefox-helper)

13. find . -name "*.txt"
    find . -type f (for directory use 'd')
    find . -type f -size 1000c
    find / -name "emacs.org" -type f 2>/dev/null

14. find . -name "*.txt" | xargs rm
    xargs -> rm expects an argument but its being passed filenames, xargs converts it into a format rm can use to delete the files 

15. sort file.txt | uniq -c

16. if [ -f file.txt ]; then
        echo "exits"
    else
        echo "missing"
    fi

    -f -> file exists
    -d -> directory exits
    -r -> readable
    -w -> writable
    -x -> executable
   

** JavaScript

 *Recap List*

1. Rewrite this as an arrow function
    function add(a, b) {
        return a + b;
    }
2. Call either function
3. Create an array of numbers
   Add a number
   Remove index 2
   Get the size of the array
4. Create a hash map containing 2 pairs
   Access a value
   Add a value to the map
5. Create a for loop that prints numbers 1 to 5
6. Create a while loop that prints the numbers 5 to 1
7. Create an if statement that checks if 'age' is 18 or over
8. Create a function called 'square' that returns the square of a given number
9. Create an arrow function called 'multiply' that returns the product of 2 numbers
10. Create an array called 'fruits' & print each element
11. Create a string variable & print its length
12. Create 2 strings & then a new variable which combines both of them together
13. Create an array called 'numbers' & remove the last element


 *Recap Answers*

1. const add = (a, b) => {
       return a + b;
   }

2. add(2, 5)  # they are both called this way

3. let numbers = [2, 6, 1, 4]
   numbers.push(7)
   numbers.splice(2, 1) # remove from index 2, remove 1 item
   numbers.length

4. let user = {
      name: "john",
      age: 25
   }
   user.name
   user.email = ~"john@gmail.com"~

5. for (let i = 1; i <= 5; i++) {
       console.log(i);
    }

6. let i = 5;

   while (i >= 1) {
       console.log(i);
       i--;
   }

7. let age = 20;

   if (age >= 18) {
       console.log("Adult");
   } else {
       console.log("Minor");
   }

8. function square(num) {
       return num * num;
   }

9. const multiply = (a, b) => {
       return a * b;
   };

10. let fruits = ["apple", "banana", "orange"];

    fruits.forEach(function(fruit) {
        console.log(fruit);
    });

11. let name = "Geoff";
    console.log(name.length);

12. let first = "Hello";
    let second = " World";

    let message = first + second;

13. let numbers = [1, 2, 3, 4];
    numbers.pop();


** Python

*Recap List*

1. Create a function called add that takes two parameters and returns their sum
2. Call the add function with the numbers 5 and 8
3. Create a list of four numbers
4. Add a number to the end of the list
5. Remove the item at index 2
6. Get the size of the list
7. Create a dictionary with the keys name and age
8. Access the value of name
9. Add an email key to the dictionary
10. Create an if statement that checks whether age is at least 18
11. Create a for loop that prints the numbers 1 to 5
12. Create a while loop that prints the numbers 5 to 1
13. Create a string called name and print its length
14. Join two strings together
15. Loop through a list and print each item
16. Create a tuple containing three numbers
17. Create a set containing three colours
18. Import the math module and print the square root of 25

*Recap Answers*

1. def add(a, b):
    return a + b

2. add(5, 8)

3. numbers = [2, 6, 1, 4]

4. numbers.append(7)

5. numbers.pop(2)

6. len(numbers)

7. user = {
       "name": "John",
       "age": 25
   }

8. user["name"]

9. user["email"] = "john@gmail.com"

10. age = 20

   if age >= 18:
       print("Adult")
   else:
       print("Minor")

11. for i in range(1, 6):
        print(i)

12. i = 5

    while i >= 1:
        print(i)
        i -= 1

13. name = "Geoff"

    print(len(name))

14. first = "Hello"
    second = " World"

    message = first + second

15. fruits = ["apple", "banana", "orange"]

    for fruit in fruits:
        print(fruit)

16. numbers = (1, 2, 3)

17. colours = {"red", "green", "blue"}

18. import math

    print(math.sqrt(25))


** Terminal Commands

 *Recap List*

1. Clear terminal with a keyboard shortcut
2. Clear current typed command
3. Put cursor at the start of the command
4. Put cursor at the end of the command
5. Go to previous directory (after a cd)
6. Rerun command as if I forgot to use sudo (i.e. apt upgrade)
7. Rerun the 3rd command I used this session
8. What's the difference between ; && and ||
9. Do (6 * 5) / 2 in the shell & return the answer
10. Search for the word 'dog' in sample.txt
11. Search how many lines of text are in the file linux.org
12. Show the first 10 lines of a linux.org & the same but the last 10
13. How do you find all the files ending in .txt in the current directory (& it's subdirectories)
14. How do you change permissions of script.sh so the owner can execute it
15. Check the last 20 commands you ran
16. Check where Python is located
17. Find the PID of the terminal
18. Kill the terminal
19. How do you stop ALL processes with the name firefox
20. Find all occurrences of the word "linux" in linux.org (ignore case sensitivity)
   

 *Recap Answers*

1. Ctrl + l

2. Ctrl + u

3. Ctrl + a

4. Ctrl + e

5. cd -

6. sudo !!

7. history
   !3

8. ;  -> run 1st command then the 2nd command regardless of whether the first succeeded or not
   && -> runs the 2nd command ONLY if the 1st succeeded
   ~||~ -> runs the 2nd command ONLY if the 1st failed

9. echo $(((6 * 5) / 2))  -> double brackets tells the shell to perform an arithmetic evaluation

10. grep "dog" sample.txt

11. wc -l linux.org

12. head -n 10 linux.org
    tail -n 10 linux.org

13. find . -name "*.txt"
    find    -> searches for files & directories
    .       -> search current directory
    -name   -> search by file or directory name
    "*.txt" -> match anything that ends with .txt

14. chmod u+x script.sh

15. history | tail -20

16. which python3

17. ps aux | grep terminal

18. kill 4567 (or whatever the number is after john-doe)

19. pkill firefox

20. grep -ci "linux" linux.org
    -c -> count
    -i -> ignore case sensitivity


* Linux (ARCH EVERYDAY -> LEARN 2 SECTIONS; LEARN 4.5)

** Troubleshooting Workflow (LEARN)

*Recap List*

1. What is the first step after an issue occurs
2. What questions should you ask to identify the problem? Give an example
3. Where should you look for error messages
4. What is journalctl and why would you use it
5. What are 3 common journalctl commands
6. After a brief search, there's no clear help, what can I do from here
7. How do I test this hypothesis
8. I'm stuck after trying a few changes, what can I do now

*Recap Answers*

1. Narrow down the issue to be as specific as possible
   For example: 'Spotify crashes immediately after opening' is much clearly than 'Spotify doesn't work'

2. Ask yourself: Can I reproduce the problem? What changed before the issue started?
   For Example: If Spotify crashes -> Does it crash every time?
                                      Does it crash when launched with the shortcut & terminal?
                                      Did the problem start after a system update or configuration change?

3. Check the terminal first
   Run the application (Spotify) from the terminal, it will return to stdout any errors

4. journalctl -> a command-line tool for viewing the system journal, this is a database of log messages maintained by systemd-jornald
                 essentially its a history of what your system has been doing

   I can use the command 'journalctl' with flags to search for specific log messages that might indicate what's caused the issue

5. journalctl -b           -> shows everything that has happened since the computer last booted (boot messages, services starting, errors, etc...)
   journalctl -xe          -> shows the most recent log messages (x = include extra explanations; e = jump to the end, i.e. newest messages)
   journalctl -u <SERVICE> -> shows only log messages for a specific app (i.e. journalctl -u spotify-launcher -> specifically for this app)

6. From a hypothesis or guess on what has caused the issue

7. Change 1 simple thing each time, test it again -> if it did not work change another thing

8. Google it, use the specific issue (Spotify crashes when I open it using the terminal) & any error messages found


** Arch Questions before Set Up

 *Recap List*

1. What is the boot process
2. What does the kernel do & give an example of the process of typing out a command
3. What is initramfs
4. What does systemd do
5. What is a bootloader & why's it needed
6. Why choose systemd-boot (instead of GRUB) & how does it work

7. What are the 3 download steps so you have a bootable Arch environment
8. What is on a .iso file specifically
9. Why do you need to write a .iso file to a USB
   
10. What is gdisk & what is a partition
11. What does it mean to format a partition
12. What filesystem did I choose & why
13. What does it mean to mount a partition
14. What is a swap partition, where do I create one
15. What is Linux LUKS & how does it work

16. What does it mean to install the base system
17. What is fstab & at what stage of boot is it used/interacted with
18. Why is EFI stored in '/boot' if the root & EFI are different partitions
19. What is mkinitcpio

   
  *Recap Answers*

1. Power on                  -> power button is pressed
   UEFI Firmware             -> initializes the hardware, does a test, reads its boot configuration & loads the bootloader from the EFI system partition
   Bootloader (systemd-boot) -> displays a boot menu, lets you choose an OS, loads the Linux kernel & loads initramfs into RAM
   Linux Kernel              -> mounts the temporary initramfs immediately, then brings the OS to live (initializes CPU features, detects hardware, etc...) 
   initramfs                 -> this helps the kernel gain access to the root filesystem (so the kernel can have the OS files)
   systemd                   -> mount remaining filesystems, start networking, start logging, start services (bluetooth, printing, etc..)
   Login                     -> the display managers prompts you to log in

2. The core of the Operating System -> over simplifying it, it deals with requests from programs
   
   When I type a command 'ls' into a terminal, the shell (bash or zsh or etc...) reads the command & starts the 'ls' program
   The 'ls' program asks the kernel, open this directory & read its contents -> the kernel reads the filesystem of disk
   Then the kernel returns the list of files, 'ls' formats the output & the terminal displays it

3. This mounts the real root filesystem -> so the kernel can access it & then the kernel takes over

   However, its a little more complicated:
   it first detects my SSD, loads encryption modules, asks for LUKs passphrase, decrypts the root partition & then mounts the partition

4. The first userspace process started by the kernel (PID 1)
   PID 1 -> mounts remaining file systems
            starts networking & logging
            launches background services (daemons)
            starts the login prompt 

5. A bootloader is the program that starts when your computer loads Linux
   This program knows how to start Linux by finding the Linux kernel, loading it to memory, passing settings & then starting it up
   It also loads the initramfs
   It can also optionally display a boot menu (GRUB or systemd-boot)
   
   To configure the bootloader means, choosing one (GRUB or systemd-boot) -> then tell the bootloader where Arch's installed,
   which kernel to boot, what options to pass & whether other OS exist

6. systemd-boot & GRUB are both bootloaders, they run immediately after the UEFI & before the OS starts
   systemd-boot is intentionally minimal, whereas GRUB is feature-rich

7. Download the Arch Linux .iso file
   Verify the download (checksum/signature) -> ensure there's no corruption
   A tool to write an ISO to a USB so its bootable
   (plug in the USB & select Arch Linux medium at the boot page)

8. A Linux kernel
   An initramfs
   The Arch live environment
   Utilities like pacman, gdisk, mkfs, mount, etc...

9. A computer can't boot from a file as the file is sitting in a file system
   The computer has to boot to the file system to access this file
   At boot, the UEFI looks for bootable disks -> the USB with a bootable .iso file can be found

10. gdisk -> a partition editor for GPT partition tables
    It lets you create, delete & resize partitions

    partition -> a reserved section of the disk (i.e. the SSD), where x amount of bytes is available for the EFI or Linux filesystem

11. After creating a partition, this is just an empty space which is reserved on the disk -> it can't store files yet
    To format a partition is to create a filesystem (ext4 or btrfs) inside that partition

12. btrfs (instead of ext4) -> it allows you to divide the partition into sub-volumes to segment sections (i.e. root, home, swap & snapshots)
    In ext4 you can't create sub-volumes, so you have to use LVM to divide up the partition

    Also, btrfs offers snapshots -> which can save a snapshot of your OS files (in case you encounter any errors after an update)

13. Mounting makes a filesystem accessible -> before, the system can't access the files

14. Swap -> disk space used as additional virtual memory, to be used when RAM becomes full

    You create a swap partition during the OS installation using a disk partitioning tool (i.e. fdisk)

15. Linux Unified Key Setup (LUKS) -> a standard disk encryption system
    So instead of having your SSD, then files, you have your files in a LUKS (encrypted) container

    Without the passphrase, Linux can't mount the filesystem because it can't understand the encrypted data (because its meaningless)

16. Copying the essential Arch Linux packages onto the new root filesystem

    Using pacstrap command, these are installed:
    Linux kernel
    The tools needed to generate an initramfs (such as *mkinitcpio*)
    systemd
    shell (bash)
    package manager (pacman)
    essential utilities

17. Filesystem Table (fstab) -> a configuration file that tells Linux which filesystems to mount automatically during boot
    i.e. UUID = /boot         UUID = /
                vfat                 btrfs

    After installing the base system, you add the UUIDs of the filesystems to the fstab directory

    On boot, during systemd -> it reads the /etc/fstab & this tells it to mount root & /boot with their UUIDs

18. /boot is a mount point -> a mount point is like a pointer, saying: from here, 'display this file system'
    So, even though the tree (of files), makes it look like /boot sits in the file tree, it actually points to the EFI file system
    i.e. clicking /boot, Linux knows is a mount point & shows you the contents of the EFI partition instead of files in a directory called 'boot'

19. A Linux utility used to create RAM filesystem (initramfs)
    mkinitcpio is the tool/program, initramfs is the file that the tool generates
    The initramfs is loaded into memory by the bootloader before the main OS starts
    The initramfs is specific to your system, the mkinitcpio gathers the correct:
        kernel modules
        firmware
        filesystem drivers
        boot script
        configuration


** Arch Set up

*Recap List*

1. Break down each section of Arch Linux setup
2. What are the 15 steps of setting up Arch after I'm in the live environment
3. First step once I'm in the live environment
4. A workflow to make it easier setting up (avoid using small text in a terminal without google)
5. For a simple set up, how many partitions are required
6. What are the sub-volumes of the LUKS container & why
7. Explain a snapshot further
8. Make a mini diagram of the root, home, swap & .snapshots files & all the containers 
9. What is arch-chroot
10. What is mkinitcpio & how does it work

*Recap Answers*

1. Arch Linux Installation

   1) Installation Media
   ├── Arch ISO
   ├── Verification
   └── Bootable USB

   1) Boot Process
   ├── UEFI
   ├── Bootloader (systemd-boot)
   ├── Linux Kernel
   ├── initramfs
   └── systemd

   1) Storage
   ├── Physical disk (SSD/NVMe)
   ├── GPT partition table
   ├── Partitions
   │   ├── EFI System Partition
   │   └── Linux partition
   ├── LUKS encryption (optional)
   ├── Filesystem (Btrfs/ext4)
   └── Btrfs subvolumes (if using Btrfs)

   1) Mounting
   ├── Root mounted at /
   ├── EFI mounted at /boot
   ├── Home mounted at /home
   ├── Snapshots mounted at /.snapshots
   ├── Swap (partition or file)
   └── fstab

   1) Base System
   ├── pacstrap
   ├── Linux kernel
   ├── systemd
   ├── bash
   ├── pacman
   └── Essential utilities

   1) Boot Configuration
   ├── mkinitcpio
   ├── initramfs
   ├── systemd-boot
   └── Boot entries

   1) System Configuration
   ├── Timezone
   ├── Locale
   ├── Hostname
   ├── Root password
   ├── User account
   ├── sudo
   └── Networking

   1) Desktop Environment
   ├── Graphics drivers
   ├── Display server (Wayland/X11)
   ├── Desktop Environment / Window Manager
   ├── Display manager
   └── Applications

   1) Maintenance
   ├── pacman updates
   ├── Bootloader updates
   ├── mkinitcpio regeneration
   ├── Snapshots
   └── Backups

2. Boot the installer -> i.e. the live environment on the USB
   SSH into the system from external PC/laptop

   Partition the disk (gdisk)
   Format partitions (mkfs)
   Encrypt the Linux partition (LUKS)
   Create the filesystem (Btrfs)
   Create Btrfs sub-volumes 

   Mount everything
   Install the base system (pacstrap)
   Generate fstab

   Chroot into the new system
   Configure the system
   Generate initramfs (mkinitcpio)
   Install the bootloader (systemd-boot)

   Reboot
   Install graphics, desktop, applications

3. Set up wifi/internet (just for the live environment

4. ssh into the Arch Linux live environment that was booted from the USB (wifi is required for this)

5. 2 partitions -> the EFI
                   the Linux LUKS container (btrfs filesystem)

6. root      -> The OS itself
   home      -> Contains personal files
   swap      -> Contain 1 file, the swap file (can alter the size of it if I'm using loads of RAM)
   snapshots -> Create save file/copy of exactly 1 sub-volume at a time (i.e. a git for sub-volumes)

7. A snapshot is actually just a pointer to the same data blocks on disk (i.e. if you had a snapshot of root, it would point to the same data as the root)
   This means, its not a copy (i.e. duplicating the data)

   Then, when a file is changed, a new block of data is written in & the filesystem points to this block, leaving the snapshot to point to the old one

8. On my 2TB SSD
   
    ├── p1  EF00
    │       EFI System Partition
    │       (unencrypted)
    │
    └── p2  8309
            LUKS encrypted container
            |
            └── Btrfs filesystem
                |
                ├── @        → root /
                ├── @home    → /home
                ├── @swap    → swap file
                └── @snapshots → snapshots

9. You use this command after the OS is installed
   It is how you shift from being in the live environment, to the Arch system (on the SSD)

10. mkinitcpio is a tool that builds the initramfs during installation (or when you update the system 'sudo pacman -Syu' mkinitcpio is run
    which creates a new initramfs-linux.img)

    It gathers everything need for early boot: kernel modules, storage drivers, filesystem drivers, LUKS tools, early /init script
    Then packs them into 1 compressed image -> initramfs-linux.img (stored in /boot/initramfs-linux.img)
    The kernel doesn't know: how to unlock LUKS, find btrfs filesystem, which modules need loading first -> it needs the initramfs


** Bandit Game (ADD MORE)

 *Recap List*

1. Explain the ssh command
2. Why can't you open a file beginning with '-'
3. Why do files or directories with spaces need quotes
4. Command to check file type
5. Command to search for files & directories
6. Command to search by word
7. Command to sort data alphabetically
8. Command to print only human-readable text
9. What does ./* mean
10. What is base64 & why would you use it
11. Command to character remap
12. What are gzip, bzip2 & tar
13. Unzip gzip, bzip2 & tar
14. 


 *Recap Answers*

1. ssh command runs secure shell client which lets you log into a remote machine
   You need to specify the port (ssh uses port 22 but the Bandit game runs on port 2220)

   ssh username@hostname -p 2220

2. Linux treats - as a special argument instead of a filename
   Use ./-<file name> so Linux knows this is a file

3. Linux treats spaces as separators between arguments
   Use " " as this tells the shell to treat everything as 1 argument

4. file <filename>

5. find <location> <flags> (i.e. find . -name <filename> -> searches current directory for a specific name)

6. grep <word> <filename>

7. sort <filename>
   
   sorts data by line alphabetically

8. strings <filename>

9. ./ -> current directory
   *  -> matches everything

   So together they mean search every file in the current directory

10. base64 is a way of converting binary data to ASCII text
    You'd use it if the data was required to be in plain-text format
    For example, an image is made up of bytes, but email is a text-only system
    You'd encode the bytes to text, then when you receive your email, the system decode the text back into the original bytes

11. tr 'characters to replace' 'characters to replace them with'

12. gzip & bzip2 -> compress files to make them smaller
    tar          -> bundle files together to make an archive

13. gunzip file.gz
    bunzip2 file.bz2
    tar -xf file.tar

    The extensions on the file names doesn't have to be correct but it really helps me work out what command I need to use to extract this file

14. 


** The File System (LEARN)

*Recap List*

1. What is the purpose of the /home directory in Linux
2. What is the purpose of the /etc directory in Linux
3. What is the purpose of the /var directory in Linux
4. What is the purpose of the /tmp directory in Linux
5. What is the purpose of the /usr directory in Linux
6. What is the purpose of the /bin directory in Linux
7. What is the purpose of the /sbin directory in Linux
8. What is the purpose of the /dev directory in Linux
9. What is the purpose of the /proc directory in Linux
10. What is the purpose of the /sys directory in Linux
11. What is the purpose of the /boot directory in Linux
12. What is the purpose of the /run directory in Linux
13. What is the purpose of the /lib directory in Linux
14. What is the difference between an absolute path and a relative path in Linux
15. What is the difference between a hard link and a symbolic link in Linux
16. How do you check if something has a symbolic link (check if /bin is a link to /usr/bin)

*Recap Answers*

1. The /home directory normally contains the personal directories of regular users

    For example:

    /home/alex
    /home/sam

    A user's home directory typically contains their personal files, documents, downloads, and configuration files

2. The /etc directory contains system-wide configuration files

    Examples include configuration files for services, networking, users, and other system settings

    For example:

    /etc/hosts
    /etc/passwd
    /etc/ssh/

3. The /var directory contains variable data, meaning data that is expected to change while the system is running

    It commonly contains things such as:

    Log files
    Spool files
    Caches
    Application data

    For example:

    /var/log/

    contains many system and application log files

4. The /tmp directory is used to store temporary files.

    Programs can use /tmp when they need somewhere to temporarily store data. Files in /tmp may be removed automatically
    when the system is restarted or according to system cleanup policies

5. The /usr directory contains many user-space programs, libraries, documentation, and other read-only/shareable data

    Common directories include:

    /usr/bin
    /usr/lib
    /usr/share

    On modern Linux systems, /usr often contains much of the software that used to be placed directly in /bin, /sbin, and related directories

6. Traditionally, /bin contains essential executable programs required for basic system operation

    Examples of commands historically found there include:

    ls
    cp
    mv
    cat

    On many modern Linux distributions, /bin is a symbolic link to /usr/bin

7. The /sbin directory traditionally contains system-administration executables, many of which are intended for system management
   On many modern Linux distributions, /sbin is a symbolic link to /usr/sbin

8. The /dev directory contains device files that represent hardware and virtual devices
   For example, disks, terminals, and other devices can be represented here

9. The /proc directory is a virtual filesystem that provides information about running processes and the Linux kernel
   For example, /proc/cpuinfo provides information about the CPU

10. The /sys directory is another virtual filesystem that provides information about hardware, devices, drivers, and the Linux kernel

11. The /boot directory contains files required to boot the Linux operating system
    This can include the Linux kernel, initial RAM filesystem files, and bootloader-related files

12. The /run directory contains temporary runtime data created since the system booted
    This can include process IDs, sockets, and other information used by running services

13. The /lib directory traditionally contains essential shared libraries and kernel modules required by programs and the operating system
    On many modern Linux systems, /lib is a symbolic link into /usr/lib

14. An absolute path starts from the root directory / and gives the complete path of a file or directory.

     Example:
     ~/home/alex/documents/file.txt~

     A relative path starts from your current directory

     Example:
     ~cd documents~ -> uses a relative path as it knows to include the path your currently in & add /documents

15. A hard link is another directory entry that refers to the same underlying file data

     A symbolic link, or symlink, is a special file that points to another file or directory

     You can create a symbolic link using:

     ~ln -s original.txt shortcut.txt~

     If the original file is removed, a symbolic link can become a broken link because it points to something that no longer exists

16. ls -ls /bin
    lrwxrwxrwx 1 root root 7 ... /bin -> usr/bin
    This shows /bin is a symbolic link to usr/bin


** My Desktop Environment (Add questions about what apps I'm using & what they do)


** Caelestia -> how it works (SORT)








* Emacs (SORT 1 SECTIONS; LEARN 5.1)

** Troubleshooting (LEARN)

*Recap List*

1. How do you check which version of Emacs is installed
2. What should you check if Emacs cannot find a file
3. What should you do if Emacs freezes or becomes unresponsive
4. How do you check if a package is installed in Emacs
5. What should you check if an Emacs package is not working
6. How do you reload your Emacs configuration file
7. What should you check if Emacs keybindings are not working
8. How do you find out what a keyboard shortcut does in Emacs
9. What should you do if Emacs opens in the wrong mode or major mode
10. How do you troubleshoot Emacs startup errors


*Recap Answers*

1. emacs --version

    Displays the version of Emacs currently installed

2. Check:
    The file path is correct
    The file actually exists
    You have permission to access it

3. Try: Ctrl + g

    This cancels the current command

    If Emacs is still frozen, close it from another terminal:
        pkill emacs

4. Open Emacs and run:
        M-x list-packages

    This displays installed packages and available packages

5. Check:

    The package is installed
    The package is enabled in your configuration file
    Required dependencies are installed
    Emacs has been restarted

    Refresh package information using:
        M-x package-refresh-contents

6. Reload the configuration file using:

        M-x load-file

    Then select your configuration file, usually:
        ~/.emacs

    or:
        ~/.emacs.d/init.el

7. Check:

    You are using the correct key combination
    The current mode has not changed the shortcut
    Another package has not overwritten the keybinding
    Your configuration file does not contain conflicting settings

    Check the current mode using:
        M-x describe-mode

8. Use: Ctrl + h k

    Then press the keyboard shortcut you want to investigate

    Emacs will display the command linked to that key combination

    Example:
        Ctrl + h k Ctrl + x Ctrl + s

    shows the command used for saving a file

9. Check the current major mode using:

        M-x describe-mode

    You can manually change the mode using:
        M-x mode-name

    Example:
        M-x python-mode

    switches the file into Python mode

10. Start Emacs without loading your configuration:

        emacs -q

    If Emacs works normally, the issue is likely in your configuration file

    For detailed startup errors:
        emacs --debug-init

    This displays errors that occur while Emacs is starting


** Org Mode

 *Recap List*

1. What is tangling
2. How have I set up my init.el to a config.org file & how does it work
3. Use 'babel' to create a block where I can write Python code, just do a print function (babel is built in, no download required)
4. What other languages can I use & what do I replace python with for each
5. How do you make it so a language can run
6. What is ':results output' for
7. What's the shortcut to run this block (between begin & end) & where should the cursor be
8. How do you move swap with the heading above & below
9. How do you promote or demote a heading
10. Syntax to make a link clickable
11. How do you create & link internal Org links
12. Click the link with a shortcut

   
 *Recap Answers*

1. The process of extracting source code from an Org file & writing it to a normal source file
   If you had a block of code between #+begin_src emacs-lisp & #+end_src
   Org mode knows to extract the Emacs Lisp code & convert it to a .el file

2. The init.el only contains code needed to load & tangle config.org
   The config.org file is located next to the init.el (~/.config/emacs/)
   Then in the config.org, write whatever comments I want & embed the Elisp in #+begin_src emacs-lisp & #+end_src

3. 
    #+begin_src python :results output
      print("hello me")
    #+end_src

4. I've set up Shell (Bash) & JavaScript in the init.el, Elisp is built in already
   But most languages are supported, I just need to add them to the init.el file

   shell
   js
   emacs-lisp

5. I have enabled python, js & shell in the config.org file so Org Babel can execute those languages

6. This tells Org Babel to capture whatever the program writes to stdout
   Without ':results output' Babel returns the value of the final expression, instead of the stdout

   For example, between a python block if I had -> print("hello")
   It would return the value of the last expression, which would be 'None' because print() returns None, hence :results output fixes this

7. C-c C-c
   The cursor can be anywhere in the block

8. M-UP
   M-DOWN

9. M-LEFT
   M-RIGHT

10. ~[[https://google.com][Google]]~ -> the only word that will appear is 'Google' underlined & blue 
    [[https://google.com][Google]]                         -> test this to load Google

11. ~<<jump-here>>~ -> this is where you jump to
    ~[[jump-here]]~ -> this is clickable & allows you to jump
    [[bash-link]]     -> test using this to jump to the bash recap list

12. C-c C-o


** Org Mode Shortcuts

*Recap List*

1. Toggle sub-tree (open & close current sub-header)
2. zr
3. Jump to next heading
4. Jump to previous heading
5. Promote & demote heading
6. Move header (or list item) up & down

*Recap Answers*

1. C-c tab
2. C-c C-n
3. C-c C-p
4. M-left & M-right
5. M-up   & M-down


** Shortcuts

*Recap List*

1. Jump down a page
2. Jump up a page
3. Jump 20 lines down
4. Jump 20 lines up
5. Pull up mini dashboard evaluate Emacs lisp expression
6. Evaluate Emacs lisp line
7. Open Emacs shell

8. List bookmarks
9. Set bookmarks

10. _Execute all source blocks, assuming document is in babel mode (i.e. to evaluate the config.org)_
11. _Place source block in its own buffer & edit this section of code_
12. How do I close the buffer & save the changes
13. _How does this new buffer work_


*Recap Answers*

1. C-f
2. C-b
3. C-d
4. C-u
5. M-:
6. C-x C-e
7. M-x shell

8. C-x r m
9. C-x r l

10. C-c C-v b
11. C-c '
12. C-c ' again
13. It creates a new buffer of just the source text in the block
    It will be in the major mode of whatever code is was (python-mode, javascript-mode, etc...)
    Therefore, Eglot will work in this buffer


** Help shortcuts

Each of the below can be used to open a new page which explains what this: shortcut, function, variable, etc... is
Give an example for each of the below (otherwise it's a little confusing)

*Recap List*

1. Describe this keyboard shortcut 
2. Describe this built-in Emacs function
3. Describe this variable
4. Check current mode (& minor modes)
5. Show the document that contains all keybindings 
6. Check mini-buffer messages


*Recap Answers*

1. C-h k (hit C-x C-s on the keyboard)
2. C-h f (type out save-buffer)
3. C-h v (setq inhibit-startup-message t -> you'd only type out 'inhibit-startup-message')
          A variable is almost always preceded by setq
4. C-h m
5. C-h b
6. C-h e
   

** Dired Mode Shortcuts

*Recap List*

1. Open file manager
2. Swap to a different directory
3. Swap sorting files (alphabetically or by timestamp)
4. Hide/reveal all but file names
   
5. Open file in a new buffer with vertical split
6. Same as above but keep focus in dired mode
7. Create directory
   
8. Change file permission
9. Change file owner
10. Change file group
    
11. Mark current file
12. Un-mark current file
13. Un-mark all files
14. Copy marked filed
15. Rename/move marked files
16. Delete marked files
17. Search marked files with grep

18. Copy file name (or at top line copy the file path)

    
*Recap Answers*

1. C-x C-j

2. C-x d

3. s

4. S-9

5. o

6. C-o

7. S-=

8. S-m

9. S-o

10. S-g

11. S-m

12. u

13. S-u

14. S-c

15. S-r

16. S-d

17. S-a

18. w


** Functionality (SORT)

 *Recap List*

1. If I have split my screen, how do I swap windows (the left one is on the right & vice versa)
2. What is package-vc-install

   
 *Recap Answers*

1. M-x window-swap-states
2. A function built into Emacs that clones packages from Git (i.e. through GitHub)
   package-install -> installs a package from a package repository
 
   
** Packages (ADD MORE)

 *Recap List*

1. What are the sources of packages in Emacs
2. Where are the files located
3. How do you search for & install a package
4. What are the steps of Emacs downloading a package
5. What does the Consult package do
6. What does the Vertico package do


 *Recap Answers*

1. There are 3: built in (internal packages)
             MELPA packages (technically still an external package but very common)
             External packages (community made ones, i.e GNU ELPA)

2. Built in -> /usr/share/emacs/29.3/lisp
   MELPA    -> ~/.config/emacs/elpa
   External -> almost always the same place as MELPA

3. M-x package-install RET <name-of-package> RET

4. You request a package -> M-x package-install
   Emacs checks the local database (~/.config/emacs/elpa/archives) -> ensure package exists
   Refresh the package list -> check for updates
   Resolve package + dependencies -> Emacs finds the package, checks dependencies & builds an install plan
   Download package files -> from the MELPA/GNU server, usually a .tar archive
   Install into local directory -> extract the .tar
   Add to load-path -> Emacs updates its internal search path so it can find the package quickly when requested
   package.el creates autoload setup -> so Emacs doesn't load the entire package until requested
   Then, you manually activate the package -> in config (require 'evil)
   Emacs then searches the load-path, finds it & loads the package into memory

5. Completion tools which gives you extra commands to help you search, navigate & select things

6. Makes the mini-buffer better by giving suggestions & options to complete what you were typing out
         
             

** Elisp

 *Recap List*

1. How do you call a function in scratch buffer
2. Define the function with name 'adding_together' & pass 2 parameters 'num1' & 'num2' & code to add them together
3. Call the function and pass 2 numbers to it
4. What is true & false in Elisp
5. What does #' mean in Emacs & where would you use it
6. Create the key-binding C-S-n & assign it to the made up function 'create-life' (convention is to use hyphens in Elisp)
7. Why would 'create-life work as well as #'create-life in the above key-binding function
8. Create the variable 'num' & pass 10 to it
9. Return to console 'num' multiplied by 3
10. What does the ' mean (give 3 examples)
11. Make a function called 'greet', which returns 'hello' & make it interactive (i.e. can be called using M-x)
12. Explain what a symbol is & how this line works -> (require 'evil)
13. What's another way to write -> #'flyspell-mode
14. Create a function that returns "hello world" & assign the shortcut C-S-h to this function
15. How do Elisp if statements work
16. Create an if statement to return 'true' or 'false'
17. Do another one to check if a variable is greater than 10


 *Recap Answers*

1. Go a line after the function is created -> C-x C-e
   Go a line after the function call       -> C-x C-e

2. (defun adding-together (num1 num2)      -> don't close the other bracket, this is closed at the end of the function 
      (+ num1 num2))

3. (adding-together 23 12)

4. true  -> t (although technically, anything that is not nil is considered true but t is standard practice)
   false -> nil

5. #' -> tells Emacs this symbol refers to a function
   So you put this before the function name to tell emacs I'm passing you a function
   #'create-life -> refers to the function (like in JavaScript, myFunction -> if you don't put () you're just referring to it, not calling it) 

6. (keymap-global-set "C-S-n" #'create-life)         ;; control shift n

   (global-set-key (kbd "C-S-n") #'create-life)      ;; identical -> legacy way of setting key-bindings

7. Because keymap-global-set expects a function, so 'create-life is passing Emacs the symbol create-life
   Emacs then looks up the function attached to that symbol

8. (setq num 10)   ;; setq -> built-in function meaning 'set variable'

9. (* num 3)       ;; C-x C-e on the variable & the same on this will return 30

10. Don't evaluate this the following expression -> known as a *quote*

    '(1 2 3) -> treats this as a list, the most common use of this key

    (setq x 10)
     x       -> returns: 10
    'x       -> returns: x

    '(+ 1 2) -> returns: (+ 1 2)

    (keymap-global-set "C-S-h" 'my-function) -> without the ', Emacs would try to evaluate my-function instead of treating it as a symbol

11. (defun greet ()            -> note: there's a space after the function name & before the brackets
        (interactive)          -> this has to be the first thing after the function is made
        (message "hello"))

    When you do M-x greet RET  -> returns: hello

12. symbol -> a variable of sorts that acts as a pointer to: variable, function or feature

    require -> is a built in function which loads this 'feature'
    'evil   -> this is treated as a symbol, not a variable (so it finds the feature named 'evil')

13. (function flyspell-mode)  -> this passes the function it DOESN'T call it

14. (defun print-hello-world ()
        (interactive)                              -> you can call this function from M-x
        (message "hello world"))
        
    (keymap-global-set "C-S-h" #'print-hello-world) -> using this key-binding, call the function (this is not in the function)

    You can do keymap-set which will make the key-binding local (rather than accessible from all modes)

15. There are no else if, there's only an if & an else (there can be just an if -> no else statement)
    (if CONDITION
        THEN              -> the code that runs if the condition is *true*
        ELSE)             -> the code that runs if the condition is *false*

16. (if t                 -> if I swap this to nil it would return: it-was-false
        'it-was-true      
        'it-was-false)

17. (setq x 15)
    (if (> x 10)
        "x is greater than 10"
        "x is equal to 10 or less")


* General Computing (LEARN 6.1, 6.2, 6.3 & 6.4)

** Data Structures & Algorithms (LEARN)

*Recap List*



*Recap List*


** Terms & Keywords (LEARN)

*Recap List*

1. What is API
2. What is TCP
3. What is UDP
4. What is DNS
5. What is FTP
6. What is SMTP
7. What does parse mean
8. What is XML

*Recap Answers*

1. An API (Application Programming Interface) is a way for different software applications to communicate with each other

    It defines a set of rules and methods that allow one program to request data or services from another program

    Example:
        A weather application uses an API to request weather information from a weather service and display it to the user

2. TCP (Transmission Control Protocol) is a networking protocol that provides reliable communication between devices over a network

    TCP:
        Ensures data arrives correctly
        Checks for errors
        Resends missing data
        Keeps packets in the correct order

    Examples of TCP usage:
        Web browsing
        Email
        File transfers

    TCP focuses on accuracy and reliability rather than speed

3. UDP (User Datagram Protocol) is a networking protocol that sends data quickly without guaranteeing delivery

    UDP:
        Does not check whether packets arrive
        Does not resend lost packets
        Has less overhead than TCP
        Is faster but less reliable

    Examples of UDP usage:
        Online gaming.
        Video streaming
        Voice calls

    UDP focuses on speed and low delay rather than reliability

4. DNS (Domain Name System) is a system that converts human-readable domain names into IP addresses

    Example:
        www.google.com

    is translated into an IP address that computers use to locate the correct server

    DNS works like a phonebook for the internet because it allows users to use names instead of remembering numerical IP addresses

5. FTP (File Transfer Protocol) is a protocol used to transfer files between computers over a network

    FTP can be used to:
        Upload files to a server
        Download files from a server
        Manage website files

    Example:
        A web developer may use FTP to upload website files to a hosting server

    A more secure alternative is SFTP (Secure File Transfer Protocol), which encrypts the connection

6. SMTP (Simple Mail Transfer Protocol) is a protocol used for sending emails between email servers

    SMTP is responsible for transferring outgoing emails from the sender's mail server to the recipient's mail server

    Example:
        When you send an email, SMTP handles the process of delivering the message to the correct email server

    Other email protocols:
        IMAP → used for retrieving and managing emails
        POP3 → used for downloading emails

7. Parsing means analysing and converting data into a structure that a computer program can understand and process

    A program parses data by breaking it down into meaningful parts

    Examples of data that can be parsed:
        JSON files
        XML documents
        User input
        Log files

8. XML (eXtensible Markup Language) is a markup language used to store, structure, and transfer data between systems

    It uses custom tags to describe the data and its meaning


** Git (LEARN)

 *Recap List*

1. What is the process of pushing a new git repository to GitHub
2. After this, how do I push a repository going forward
3. If I have a folder connect to a GitHub remote repository, how do I update the files
4. What would I do if the local repository is connected to the wrong remote repository
5. Explain 'git push -u origin main'
6. What is the purpose of .gitignore in a Git repository
7. What is the difference between git fetch and git pull
8. What is a merge conflict in Git, and how do you resolve one
9. What is the difference between git reset and git revert
10. Go through the quick memory quide

   
 *Recap Answers*

1. git init                       -> initialise the repository (create the .git folder & its files)
   git add .                      -> stage all of the files
   git commit -m "initial commit" -> create a commit
   git branch -M main             -> Git creates the first branch as master, but GitHub uses main (this changes the name of the branch)
                                     the -M flag forces the rename
   git remote add origin git@github.com:benwood256/<repository name>.git
   git push -u origin main        ->  push local repository (main) to the remote one (origin)

2. git push (everything was set up before, git knows where to send the local repository)
   
3. git pull

4. git remote set-url origin git@github.com:benwood256/<repository name>.git

5. git push -> push local commit to a remote repository
   -u       -> remember that my local 'main' branch should track the remote called 'origin'
               without this, I'd have to specify the destination every time (now I can use 'git push' each subsequent push)
   origin   -> name of the remote repository
   main     -> the local branch you're pushing

6. .gitignore is a file that tells Git which files or folders not to track

    It is commonly used for:
        Passwords/secrets
        Temporary files
        Dependencies
        Configuration files

    Example:
        node_modules/
        .env

7. git fetch and git pull both download changes from a remote repository

    git fetch:
        Downloads changes but does not apply them
        Lets you review changes before merging

    git pull:
        Downloads changes and automatically merges them into your current branch

    git pull is basically:
        git fetch + git merge

8. A merge conflict happens when Git cannot automatically combine changes because two branches have edited the same part of a file differently

    To resolve it:
        Open the conflicting file
        Choose which changes to keep
        Remove the conflict markers
        Commit the resolved changes

9. git reset and git revert both undo changes, but they work differently

  git reset:
      Moves the branch backwards
      Can remove commits from history
      Mainly used for local changes

  git revert:
      Creates a new commit that undoes a previous commit
      Keeps the existing history
      Safer for shared repositories

10. Quick memory guide:

        git add    → prepare changes
        git commit → save changes locally
        git push   → upload changes
        .gitignore → ignore unwanted files
        git fetch  → download changes only
        git pull   → download + merge
        git reset  → move history backwards
        git revert → undo with a new commit


** Regex (LEARN)

*Recap List*

1. What is a regular expression (Regex), and what is it used for
2. What is the difference between a literal character and a special character in Regex
3. What does the . (dot) character mean in Regex
4. What do the ^ and $ symbols mean in Regex
5. What are character classes in Regex, and how are they written
6. What is the difference between [abc] and [^abc] in Regex
7. What do the Regex quantifiers *, +, and ? mean
8. What do the quantifiers {n}, {n,}, and {n,m} mean in Regex
9. What is the difference between greedy and lazy matching in Regex
10. What are capturing groups in Regex, and how are they created
11. What is the difference between \d, \w, and \s in Regex
12. What are Regex flags, and what are some common examples
13. How would you write a Regex pattern to validate an email address
14. How would you use Regex to find and replace text
15. What is the difference between match, search, and replace when using Regex

*Recap Answers*

1. A regular expression (Regex) is a sequence of characters used to define a search pattern
   It is used to find, validate, extract, or replace text based on specific rules

    Common uses include:

    Validating emails
    Finding phone numbers
    Searching logs
    Replacing text
    Extracting information from large amounts of data

        Example:
        cat

    This matches the exact text "cat"

2. A literal character matches itself exactly

        Example:
        hello

    Matches:
    hello

    A special character has a special meaning in Regex

    Examples:
    .  *  +  ?  ^  $  [ ]

        For example:
        .

    does not mean a literal dot — it means "any character"

    To match a real dot, you escape it:
    \.

3. The . (dot) character matches any single character except a newline (unless a special flag is enabled)

        Example:
        h.t

    Matches:
    hat
    hot
    hit

    but not:
    heat

    because the dot only represents one character

4. The ^ and $ symbols are anchors

    ^ means the beginning of a string

        Example:
        ^Hello

    Matches:
    Hello world

    but not:
    Say Hello

    $ means the end of a string.

        Example:
        world$

    Matches:
    Hello world

    but not:
    world today

5. Character classes allow you to match one character from a set of possible characters

    They are written using square brackets:

    [abc]

    This matches:
    a
    b
    c

        Example:
        [0-9]

    Matches any single digit from 0 to 9

6. [abc] matches any character listed inside the brackets

        Example:
        [abc]

    Matches:
    apple
    ball
    cat

    [^abc] means not a, b, or c

        Example:
        [^0-9]

    Matches any character that is not a number

7. Regex quantifiers specify how many times a character or group can appear

   * = zero or more times

        Example:
        ab*

    Matches:
    a
    ab
    abb
    abbb

    + = one or more times

        Example:
        ab+

    Matches:
    ab
    abb
    abbb

    but not:
    a

    ? = zero or one time

        Example:
        colou?r

    Matches:
    color
    colour

8. Quantifiers with curly brackets specify exact amounts

    {n} = exactly n times

        Example:
        \d{4}

    Matches exactly four digits:
    2026

    {n,} = at least n times

        Example:
        \d{2,}

    Matches:
    12
    123
    12345

    {n,m} = between n and m times

        Example:
        \d{2,4}

    Matches:
    12
    123
    1234

9. A greedy match tries to match as much text as possible

        Example:
        <.*>

        Given:
        <a>Hello</a>

        It may match:
        <a>Hello</a>

    A lazy match tries to match as little as possible

    You create it by adding ?

        Example:
        <.*?>

    It matches:
    <a>

    then stops at the first possible match

10. Capturing groups allow you to group parts of a Regex and remember the matched text

    They are created using parentheses:
    (\d{3})-(\d{4})

        Example:
        123-4567

    The Regex captures:
    Group 1: 123
    Group 2: 4567

    Groups are useful when extracting specific parts of text

11. \d matches any digit:

    Equivalent to:
    [0-9]

        Example:
        123

    \w matches word characters:
    letters
    numbers
    underscore

        Equivalent to:
        [A-Za-z0-9_]

        \s matches whitespace

    Examples:
    space
    tab
    newline

12. Regex flags modify how a pattern behaves

    Common flags include:
    i — case insensitive

        Example:
        hello/i

    Matches:
        Hello
        HELLO
        hello

    g — global search (find all matches)
    m — multiline mode

13. A basic email validation Regex could be:

    ^[\w.-]+@[\w.-]+\.\w+$

    Explanation:
    ^ → start of string
    [\w.-]+ → username characters
    @ → required @ symbol
    [\w.-]+ → domain
    \. → literal dot
    \w+ → domain extension
    $ → end of string

    Example matches:
    alex@example.com
    user123@test.co.uk

14. Regex can be used with replace functions to find text and substitute it

        Example in JavaScript:
        let text = "Hello World";

        text.replace(/World/, "Linux");

        Result:
        Hello Linux

    Regex makes replacements more powerful by allowing patterns instead of exact text

15. match() finds text that matches a Regex pattern and returns the matches

        Example:
        "abc123".match(/\d+/);

        Returns:
        123

    search() finds the position of the first match.

        Example:
        "hello123".search(/\d/);

        Returns:
        5

    replace() finds matching text and replaces it.

        Example:
        "hello123".replace(/\d+/,"456");

        Returns:
        hello456

    So:
    match → find and return matches
    search → find the location
    replace → find and substitute text


* Python

** Mini Problems (ADD MORE)

 *Recap List*

1. Find all the factors (not including) of a number
2. Filter a string to only get the letters
3. Check if a string contains only numbers
   

 *Recap Answers*

1. def factors(number):
      all_factors = []
      for value in range(1, number // 2 + 1):
         if number % value == 0:
            all_factors.append(value)
      return all_factors

2. def letter_string(string):
      only_letters = ""
      for letter in string:
         if letter.isalpha():        # isalpha() returns true is if every character in a string is a letter
            only_letters += letter
      return only_letters

3. def check_number(string):
      return string.isdigit()        ## isdigit() returns true is every character in a string is a digit

      
** OOP

 *Recap List*

1. What is a method
2. What are the 4 pillars of OOP
3. What is abstraction with an example (& why is an _ needed before a variable name)
4. What is encapsulation with an example
5. What is inheritance with an example
6. What is polymorphism with an example
7. What's the difference between a class & an object
8. What's the difference between a class attribute & instance attribute, with an example


 *Recap Answers*


1. A function that's embedded in a class

2. Encapsulation, Abstraction, Inheritance & Polymorphism

3. Using functions to change a variable, rather than directly changing it
   Its typical to use an underscore before the variable name, to say -> don't directly change this value

   class Thermostat:
      def __init__(self):
         self._temperature = 20

      def increase(self):
         self._temperature += 1

      def check_temperature(self):
         return self._temperature

   thermostat = Thermostat()
   thermostat.increase()
   print(thermostat.check_temperature()) -> outputs 21

   Notice, at no point do you do thermostat._temperature = 21 but you use the methods to interact with it

4. If I created a game
   while game_running:
      enemy.take_damage()   -> this calls a method from the enemy class
   This is abstraction, the alternative is writing the code there
   i.e. health -= 30, an if loop to check if this means the player dies, call function to play death sound, etc...

   To me, this is a fancy word for using methods & making complex classes & calling those classes & methods when needed
   Rather than writing the code in order, when its required

5. One class can reuse another classes functions

   class Animal:
      def speak(self):
         print("hello there")

   class Dog(Animal):
      pass

   # Dog has access to Animal's functions

6. Classes can have the same name for functions, this can be used in a function that requires the name of a class

   class Dog:
      def speak(self):
         return "woof"

   class Cat:
      def speak(self):
         return "meow"

   def make_noise(animal):
      print(animal.speak())    # requires a class to be passed to it

   dog = Dog()
   cat = Cat()

   make_noise(dog)             # output -> woof
   make_noise(cat)             # output -> meow

7. Class  -> the blueprint that can be used over and over
   Object -> 1 instance of a class/blueprint

8. Instance attribute is if you create the variable but have the object set it

   class Player:
      def __init__(self, name):
         self.name = name

   player1 = Player("max")            -> I created the name, so each time an object is made it will need to set a unique name

   A class attribute is if the self.name = "max", so every object will have the same name


** Magic Methods (ADD MORE)

 *Recap List*

1. Explain init & how it works
2. Explain str & how it works, with an example
3. Explain repr & how it works

   
 *Recap Answers*


1. Called when the object is created (i.e. creating variables)

2. Allows you to print what's stored in an object -> otherwise, Python would print the objects location

   class Player:
      def __init__(self, name):
         self.name = name

      def __str__(self):
         return self.name

   player = Player("Alice")
   print(player)                  # output -> Alice

3. repr is very similar to str, both allow you to print objects to the terminal
   But, repr is used for debugging (i.e. used for programmers)
   Think how I embed a console.log("this_function is working) in JavaScript, repr is like that but for a class, to check what's outputted 


** Pygame (ADD MORE)

 *Recap List*

1. What's required at the start of the file, to start/setup each game
2. Write out the main game function
3. What goes in the main function, give examples
4. Where should global variables go & what's the best practice
5. Why do you need a game clock, where would you make the clock & show how to make it


 *Recap Answers*


1. import pygame
   create the screen (dimensions) -> win = pygame.display.set_mode((2000, 2000))
   give the screen a caption      -> pygame.display.set_caption("pong")            # this will appear at the top of the window
   create a function (main()) to run the game & call this at the bottom of the page
   within the main() function, create a while loop to check if the player tries to 'x' off the screen

2. def main()
      run = True

      while run:
         for event in pygame.event.get():
            if event.type == pygame.QUIT:   # pygame.QUIT -> the 'x' button was clicked to exit the page
               run = False
               break                        # this will work without the break, the break is just a good habit

      pygame.quit()                         # when run is False, the game quits

3. Everything that can make things happen in the game (player moves, enemy moves, projectiles, etc...)

4. Put them before the game loop & in _all caps_, so I know they are global

5. Otherwise game speed is entirely linked to the PC output
   It should be created before the main() -> CLOCK = pygame.time.Clock()
   & then inside - while run: 
                      clock.tick(60)      # game will now run at 60 FPS
